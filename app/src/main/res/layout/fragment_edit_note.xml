<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorPrimaryDark"
    android:orientation="vertical">

        <EditText
            android:id="@+id/edit_note_edtitext"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:singleLine="false"
            android:text=""
            android:scrollbarAlwaysDrawVerticalTrack="true"
            android:gravity="fill_horizontal|top|left"
            android:textColor="@color/colorEditNote"
            android:textSize="15sp"
            android:imeOptions="flagNavigateNext"
            android:textCursorDrawable="@drawable/color_cursor_edit_frag"
            android:inputType="textMultiLine|textAutoCorrect|textLongMessage"
            android:background="@android:color/transparent"
            />

</LinearLayout>